// Phase 2 entry: full renderer port. `renderer.js` (ported from
// `src/renderer/renderer.js` with the tauriApi shim + asset:// paths +
// native drag-drop) owns all UI; this module just boots it.
import "./renderer.js";
